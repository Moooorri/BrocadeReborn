# SongBrocade 数据统计设置教程

这份教程只需要做一次。代码已经会自动发送访问和保存事件，你需要手动创建数据库，并把密钥填到 Netlify。

## 你会收集到什么

- `visit`：用户打开网页时记录一次。
- `save_palette`：开发版点击“保存色系”时记录。
- `save_share_image`：点击分享弹窗里的“保存”时记录。

保存事件会包含纹样编号、骨架排版、骨架线型、图元编号、地纹、5 个色系颜色、地纹色、背景色等。

## 第 1 步：创建 Supabase 项目

1. 打开 <https://supabase.com/>。
2. 登录后点 `New project`。
3. Organization 选默认即可。
4. Project name 可以填 `SongBrocade Analytics`。
5. Database Password 自己设置一个强密码，保存好。
6. Region 尽量选离访问者近的区域。
7. 点创建，等项目初始化完成。

## 第 2 步：建数据表

进入 Supabase 项目后：

1. 左侧点 `SQL Editor`。
2. 点 `New query`。
3. 粘贴下面这段 SQL。
4. 点 `Run`。

```sql
create table if not exists public.analytics_events (
  id bigint generated by default as identity primary key,
  created_at timestamptz not null default now(),
  event_type text not null,
  visitor_id text,
  page text,
  referrer text,
  user_agent text,
  language text,
  screen jsonb not null default '{}'::jsonb,
  payload jsonb not null default '{}'::jsonb
);

create index if not exists analytics_events_created_at_idx
  on public.analytics_events (created_at desc);

create index if not exists analytics_events_event_type_idx
  on public.analytics_events (event_type);

create index if not exists analytics_events_pattern_id_idx
  on public.analytics_events ((payload->>'patternId'));
```

## 第 3 步：复制 Supabase 密钥

在 Supabase 项目里：

1. 左下角点 `Project Settings`。
2. 点 `API`。
3. 复制 `Project URL`，它长得像 `https://xxxxx.supabase.co`。
4. 复制 `service_role` key。

注意：`service_role` 是后台密钥，不要发到网页里，不要写进 GitHub，只填到 Netlify 环境变量。

## 第 4 步：把密钥填到 Netlify

进入 Netlify 里这个网站的项目：

1. 点 `Site configuration` 或 `Site settings`。
2. 找到 `Environment variables`。
3. 新增两个变量：

```text
SUPABASE_URL=你的 Project URL
SUPABASE_SERVICE_ROLE_KEY=你的 service_role key
```

4. 保存。
5. 回到 Deploys，点 `Trigger deploy`，选择重新部署。

## 第 5 步：检查是否成功

1. 打开部署后的网站。
2. 刷新一次页面。
3. 点击分享里的“保存”。
4. 回到 Supabase，打开 `Table Editor`。
5. 查看 `analytics_events` 表，应该能看到 `visit` 和 `save_share_image`。

## 常用查询

总访问次数：

```sql
select count(*) as visits
from public.analytics_events
where event_type = 'visit';
```

每天访问次数：

```sql
select date_trunc('day', created_at) as day, count(*) as visits
from public.analytics_events
where event_type = 'visit'
group by day
order by day desc;
```

保存次数：

```sql
select count(*) as saves
from public.analytics_events
where event_type in ('save_palette', 'save_share_image');
```

被保存的纹样：

```sql
select
  payload->>'patternId' as pattern_id,
  payload->>'skeletonLayoutName' as layout_name,
  payload->>'skeletonStyleName' as skeleton_style,
  payload->'paletteColors' as palette_colors,
  created_at
from public.analytics_events
where event_type in ('save_palette', 'save_share_image')
order by created_at desc;
```

最常被保存的骨架排版：

```sql
select payload->>'skeletonLayoutName' as layout_name, count(*) as saves
from public.analytics_events
where event_type in ('save_palette', 'save_share_image')
group by layout_name
order by saves desc;
```

## 隐私建议

毕业论文使用时，建议只收集“交互和生成参数”，不要收集姓名、手机号、微信、邮箱等个人身份信息。页面可以加一句简短说明：

> 本作品会匿名记录访问、保存操作与纹样生成参数，用于毕业设计研究与作品优化，不收集姓名或联系方式。

